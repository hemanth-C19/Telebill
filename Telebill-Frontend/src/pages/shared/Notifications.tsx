// Shared notifications page used by all portals — lists user notifications with filter and mark-read actions

export default function Notifications() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-gray-900">Notifications</h1>
      <p className="mt-2 text-sm text-gray-500">Notification center (placeholder).</p>
    </div>
  )
}
